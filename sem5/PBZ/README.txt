В database.py в функции create_tables нужно будет перезадать путь к файлу SQL_PBZ.sql

А так же
Установить системные зависимости
sudo apt update
sudo apt install python3-pip postgresql libpq-dev

Затем Python библиотеки
pip install Flask psycopg2-binary


